import { ElementStates } from "../types/element-states";
export type TArrayString = {
  value: string;
  state: ElementStates;
};
export type TArraySorting = {
  value: number;
  state: ElementStates;
};
